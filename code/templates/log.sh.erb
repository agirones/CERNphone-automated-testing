#!/bin/bash

FILE=$(date +\%y-\%m-\%d_\%H:\%M)
/root/run.sh > /var/log/volts/$FILE.log
cp /root/tmp/output/result.jsonl /var/log/volts/$FILE.jsonl

rm $(find . -mtime +<%= @logs_backup_days %>) 2>/dev/null
